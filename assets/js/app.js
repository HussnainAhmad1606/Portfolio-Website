// Typing Effect on Homepage

var options = {
	strings:
	["Websites","Softwares","Games"],
	startDelay:600,
	backDelay:600,
	typeSpeed:100,
	backSpeed:70,
	loop:!0}

typed=new Typed("#role",options);
AOS.init({duration:1200});